{% extends 'base.html.twig' %}

{% block title %}Tuto{% endblock %}

{% block body %}
    <h1>Tuto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tuto.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ tuto.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ tuto.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tuto_index') }}">back to list</a>

    <a href="{{ path('tuto_edit', {'slug': tuto.slug}) }}">edit</a>

    {{ include('tuto/_delete_form.html.twig') }}
{% endblock %}
